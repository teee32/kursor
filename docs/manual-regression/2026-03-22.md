# Manual Regression Report - 2026-03-22

## Scope

This round covers the current uncommitted changes on top of `main`, with focus on the newline handling added in `src/inline/InlineProvider.ts` and the related command and packaging updates.

Files in scope:

- `src/inline/InlineProvider.ts`
- `package.json`
- `scripts/check-node.js`
- `README.md`
- `media/icon.svg`

## Build Under Test

- Base commit: `b739bb8`
- Working tree: local uncommitted changes present
- Compile status: `npm run compile` passed on 2026-03-22
- Packaging gate: `npm run verify-node` failed as expected on local Node `v18.19.1`; packaging now requires Node `20.18.1+`

## Environments

- Browser-hosted editor session, evidence captured on 2026-03-21 23:21 to 23:53 +08:00
- Desktop VS Code session, evidence captured on 2026-03-22 00:02 +08:00

## Executed Checks

| Area | Result | Evidence |
| --- | --- | --- |
| Command registration | Pass | `vscode-kursor-commands.png` shows `Clear Chat`, `Inline Chat`, `Set Kimi API Key`, and the chat view entry |
| Missing API key path | Pass | `vscode-desktop-missing-key.png` shows the chat panel returning an actionable "No API Key" message |
| Set API key flow | Pass | `vscode-desktop-chat-success.png` shows the API key saved toast after configuration |
| Chat sidebar response | Pass | `kursor-chat-success.png` shows a prompt and returned assistant response in the chat view |
| Clear chat/reset state | Pass | `kursor-clear-success.png` shows the welcome state after clearing the conversation |
| Inline chat postfix insertion regression | Pass | `kursor-postfix-inline-sample2-fixedmock.png` shows the generated line inserted on a new line instead of being appended to the existing statement |
| Desktop inline chat sanity | Pass | `vscode-desktop-inline-success.png` shows inline generation producing a separate `console.log(result)` line and the completion toast |

## Evidence Files

- `docs/manual-regression/2026-03-22/kursor-chat-success.png`
- `docs/manual-regression/2026-03-22/kursor-clear-success.png`
- `docs/manual-regression/2026-03-22/kursor-postfix-inline-sample2-fixedmock.png`
- `docs/manual-regression/2026-03-22/vscode-kursor-commands.png`
- `docs/manual-regression/2026-03-22/vscode-desktop-missing-key.png`
- `docs/manual-regression/2026-03-22/vscode-desktop-chat-success.png`
- `docs/manual-regression/2026-03-22/vscode-desktop-inline-success.png`

## Notes

- The key risk in this change set was postfix inline insertion. That path is now explicitly covered with evidence from both a browser-hosted editor session and a desktop VS Code session.
- The old broken behavior was reproduced before the formatter change, where generated code was appended inline to the existing statement. The captured pass evidence above reflects the fixed behavior only.

## Gaps

- `npm run package` was not rerun after the new Node gate because the current local runtime is `v18.19.1`, below the required `20.18.1+`.
- The `selectedText` replacement branch in `InlineProvider` was not explicitly re-captured in this round's screenshots.
- There is still no automated end-to-end regression suite in the repository; this report is evidence of manual validation only.

## Exit Assessment

Manual regression is sufficient for the current change set's primary risk areas: command discovery, missing-config handling, chat response, clear-chat reset, and postfix inline insertion formatting.

The release is not fully closed for packaging until the same build is rerun on Node `20.18.1+`.
